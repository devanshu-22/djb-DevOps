apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: minio-dev-djberp-cert
  namespace: backbone
spec:
  secretName: minio.dev-djberp.nitcon.in-tls-certs
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - minio.dev-djberp.nitcon.in