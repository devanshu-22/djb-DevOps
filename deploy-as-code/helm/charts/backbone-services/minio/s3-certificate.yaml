apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: s3-dev-djberp
  namespace: backbone
spec:
  secretName: s3.dev-djberp.nitcon.in-tls-certs
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - s3.dev-djberp.nitcon.in